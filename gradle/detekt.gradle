apply plugin: "io.gitlab.arturbosch.detekt"

detekt {
    parallel = true
    config = files("$rootProject.projectDir/detekt/detekt.yml")
    reports {
        html.enabled = true
        xml.enabled = false
        txt.enabled = false
    }
}
